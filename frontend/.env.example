# Token GitHub pour publier les releases Electron (electron-builder --publish)
# Permissions requises : repo (lecture/écriture)
# GitHub → Settings → Developer settings → Personal access tokens
GH_TOKEN=

# URL API backend (optionnel en dev)
VITE_API_BASE_URL=http://127.0.0.1:8002
